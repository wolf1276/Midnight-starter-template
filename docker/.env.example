# Generated automatically by ./setup.sh — copy this file to docker/.env only if you're
# bypassing setup.sh and need to set it up by hand.
#
# 64 hex chars (32 bytes), e.g.: openssl rand -hex 32
INDEXER_SECRET=
